# Cloudflare Pages Configuration
name = "smssub-website"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# REQUIRED: Output directory for Pages
pages_build_output_dir = "."

# Build command to install dependencies
[build]
  command = "npm install"

# Local development
[dev]
  port = 8788
